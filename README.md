# Programming-fundamentals-project
This C++ project performs basic matrix operations including addition, subtraction, and multiplication. It dynamically allocates memory for two user-defined matrices, takes their elements as input, and  carries out the operations accordingly. The program also ensures proper memory deallocation and displays the resulting matrices clearly.
